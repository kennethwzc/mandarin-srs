#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validate commit message format using commitlint
# This ensures all commits follow Conventional Commits specification
# See: .github/COMMIT_CONVENTION.md for format details
npx --no -- commitlint --edit ${1}
